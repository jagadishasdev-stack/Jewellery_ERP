# Copy to .env and fill in real values before running `docker compose up`.
# Never commit the real .env — it's gitignored already.
DB_PASSWORD=
JWT_SECRET=
JWT_REFRESH_SECRET=
CLIENT_URL=http://localhost:3000
